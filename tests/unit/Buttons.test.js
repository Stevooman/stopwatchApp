import { mount } from '@vue/test-utils';
import Buttons from './src/components/Buttons.vue';
import { describe, expect } from 'vitest';

describe('Buttons.vue', () => {
  test('Clicking the button triggers proper method', () => {
    
  });
});